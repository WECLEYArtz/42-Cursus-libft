#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(){
	// char	*test = malloc(1);
	// test[0] = 0;
	// test[1] = 0;
	char	*test = 0;
	test = NULL;
	

	// char	*test =strdup("");
	printf("what: %s",test);
}
